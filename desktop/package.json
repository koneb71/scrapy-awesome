{
  "name": "scrapy-awesome-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Desktop shell (Tauri v2) for scrapy-awesome — wraps the frozen Python sidecar",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "icons": "tauri icon icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
